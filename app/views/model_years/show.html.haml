.row
  .large-12.columns
    %h3
      = link_to "All Makes", root_path
      >
      = @name

    %hr

.row
  .large-10.large-centered.columns
    = image_tag(get_sample_image_for_style(@styles.first["subModels"].first["styleIds"]))

  %p
  - @styles.each do |style|
    .large-10.large-centered.columns
      = link_to style["attributeGroups"]["STYLE_INFO"]["attributes"]["STANDARD_VEHICLE_DESCRIPTION"]["value"], style_detail_path(id: style["id"]), :class => "button expand"




