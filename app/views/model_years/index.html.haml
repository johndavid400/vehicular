%h3
  = link_to "All Makes", root_path
  >
  = link_to @make, make_path(name: @make, id: @model_years.first["makeId"])
  >
  = @model


%hr

- @model_years.sort{|x,y| y["year"] <=> x["year"] }.each do |year|
  .row
    .large-6.columns
      = link_to year["year"], "#", :class => "button large expand", "data-reveal-id" => year["year"]
      %div{:id => year["year"], :class => "reveal-modal"}
        %h4 Sub Models
        - year["subModels"].each do |sub|
          = sub["name"]
          %p


